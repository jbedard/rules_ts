# See https://docs.aspect.build/workflows/configuration
workspaces:
    .:
        icon: typescript
        label: rules_ts
    e2e/smoke:
        tasks:
            - bazel-6:
                  without: true
            - bazel-7:
                  queue: aspect-medium
            - bazel-8:
                  queue: aspect-medium
            - format:
                  without: true
            - buildifier:
                  without: true
    e2e/bzlmodules:
        tasks:
            - bazel-6:
                  without: true
            - bazel-7:
                  queue: aspect-medium
            - bazel-8:
                  queue: aspect-medium
            - format:
                  without: true
            - buildifier:
                  without: true
    e2e/rules_js_v3:
        tasks:
            - bazel-6:
                  without: true
            - bazel-7:
                  queue: aspect-medium
            - bazel-8:
                  queue: aspect-medium
            - format:
                  without: true
            - buildifier:
                  without: true
    e2e/external_dep:
        tasks:
            - bazel-6:
                  queue: aspect-medium
            - bazel-7:
                  queue: aspect-medium
            - bazel-8:
                  queue: aspect-medium
            - format:
                  without: true
            - buildifier:
                  without: true
    e2e/worker:
        tasks:
            - bazel-6:
                  queue: aspect-medium
            - bazel-7:
                  queue: aspect-medium
            - bazel-8:
                  queue: aspect-medium
            - format:
                  without: true
            - buildifier:
                  without: true
    e2e/test:
        tasks:
            - bazel-6:
                  queue: aspect-medium
            - bazel-7:
                  queue: aspect-medium
            - bazel-8:
                  queue: aspect-medium
            - format:
                  without: true
            - buildifier:
                  without: true
    examples:
        tasks:
            - bazel-6:
                  without: true
            - bazel-7:
                  # Uses REPO.bazel to exclude node_modules
                  without: true
            - bazel-8:
                  queue: aspect-medium
            - format:
                  without: true
            - buildifier:
                  without: true
tasks:
    - test:
          name: Bazel 6
          id: bazel-6
          bazel:
              flags:
                  - --incompatible_merge_fixed_and_default_shell_env
          env:
              USE_BAZEL_VERSION: 6.5.0
    - test:
          name: Bazel 7
          id: bazel-7
          env:
              USE_BAZEL_VERSION: 7.x
    - test:
          name: Bazel 8
          id: bazel-8
          env:
              USE_BAZEL_VERSION: 8.x
    - test:
          name: Bazel 9
          id: bazel-9
          env:
              USE_BAZEL_VERSION: 9.x
    - format:
          queue: aspect-medium
          use_args_file: false
    - buildifier:
          queue: aspect-medium
    - finalization:
          queue: aspect-small
notifications:
    github: {}
