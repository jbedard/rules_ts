load("//ts:defs.bzl", "ts_project")

ts_project(
    name = "compile",
    srcs = [":c.ts"],
    composite = True,
    extends = "//:tsconfig-base.json",
    tsconfig = "tsconfig.json",
    deps = ["///b"],
)
